<h1>Listado de Recetas</h1>
<hr />
<% if (@recetas.nil? or @recetas == []) %>
<h2>No Hay recetas</h2>
<% else %>
<h3>Su Query fue: <%= @param %></h3>
<table align="center">
  <tr style="background-color: black;color:white">
    <th>Thumbnail</th>
    <th>Titulo</th>
    <th>Ranking Social</th>
  </tr>
  <% @recetas.each do |receta| %>
    <tr class='<%= cycle('even', 'odd') %>'>
      <td><div class="zoom frame"><%= link_to image_tag(receta["image_url"], size: "100x100"), receta["source_url"] %></div></td>
      <td><%= link_to sanitize(receta["title"]), receta["source_url"],{ :class=>"link" } %></td>
      <td><%= receta["social_rank"] %></td>
    </tr>
  <% end %>
</table>
<hr />
<br/>
implementado por Jose Luis Quispe Aracayo
<% end %>